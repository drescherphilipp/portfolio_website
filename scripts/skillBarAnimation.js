$(function () { // wait for document ready
    var trigger_pos = 0.8;
    var controller = new ScrollMagic.Controller();

    // header_skills
/*
    var typewriter_skills = new Typewriter(document.getElementById('header_skills'), { loop: false });
    var scene_skills = new ScrollMagic.Scene({triggerElement: '#header_skills', triggerHook: 1})
    .on("enter", function (event) {
      typewriter_skills.typeString('Skills').start();
    })
    .on("leave", function(event) {
      typewriter_skills.stop();
    })
    .addTo(controller);
*/

});